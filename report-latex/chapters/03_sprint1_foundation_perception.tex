\chapter{Sprint 1: Foundation and Perception}
\chaptertoc

\clearpage

\section{Introduction}
Sprint 1 built the first perception flow of the system. It takes a video stream, identifies people, keeps track of them across frames, and limits counting to the queue area. The goal of this sprint was to prepare clean and reliable input for the later analysis stages.

\section{Sprint Backlog}
The Sprint 1 backlog focused on the basic perception flow:
\begin{itemize}
    \item Detect people in the video stream while keeping the detector adjustable to different machines.
    \item Keep the same person identifiable across frames so the counts stay consistent.
    \item Define queue zones as polygons and count only the people inside these zones.
    \item Connect capture, detection, tracking, zone filtering, and display into one continuous flow.
    \item Allow basic runtime settings so the pipeline can be adapted without changing the code.
\end{itemize}

\section{Sprint Analysis}

\subsection{Detailed Use Case Diagram}
\begin{figure}[H]
    \centering
    \fbox{\parbox[c][6cm][c]{0.85\textwidth}{\centering Placeholder for Sprint 1 use case diagram}}
    \caption{Sprint 1 use case diagram for detection and tracking in queue zones.}
    \label{fig:sprint1_use_case}
\end{figure}

The use case starts when a manager sets up a source and defines the queue area. After that, the runtime processes each frame and updates the queue state. This gives the later analytics layers a stable perception base to work with.

\subsection{Use Case Scenarios}
Three key scenarios are implemented in Sprint 1:
\begin{enumerate}
    \item \textbf{Person entry into queue zone:} when a person enters the queue area, the system treats that person as a possible arrival.
    \item \textbf{Consistency across frames:} when the same person appears in consecutive frames, the system keeps the identification stable and avoids double counting.
    \item \textbf{Zone filtering under mixed scenes:} people outside the queue area are ignored so the queue state stays focused on the relevant part of the scene.
\end{enumerate}

\section{Design}

\subsection{Activity Diagram}
\begin{figure}[H]
    \centering
    \fbox{\parbox[c][6cm][c]{0.85\textwidth}{\centering Placeholder for Sprint 1 activity diagram}}
    \caption{Sprint 1 activity flow from frame ingestion to queue-zone updates.}
    \label{fig:sprint1_activity}
\end{figure}

The activity starts from a video source, whether it is a local file or a live camera stream. The system then detects people, keeps track of them, checks whether they are inside the queue area, and passes the filtered result to the next analysis stage.

\subsection{Sequence Diagrams}
\begin{figure}[H]
    \centering
    \fbox{\parbox[c][6cm][c]{0.85\textwidth}{\centering Placeholder for Sprint 1 sequence diagram}}
    \caption{Sprint 1 sequence diagram from video ingestion to queue-state updates.}
    \label{fig:sprint1_sequence}
\end{figure}

In practice, each frame moves through source handling, detection, tracking, zone checking, and then the queue analysis stage. That ordered flow is what lets Sprint 1 feed the later metric work.

\section{Implementation}
\subsection{Detection Layer}
The detection layer focuses on finding people in each frame while keeping the process adjustable for different machines. It is tuned to balance speed and accuracy, so the system stays responsive without losing the main detections that matter for the queue.

\subsection{Tracking Layer}
The tracking layer keeps the same person identifiable from one frame to the next. This is what makes arrival and departure counts more reliable than simply reading detections in a single frame \cite{bytetrack2022}.

\subsection{Zone-Aware Filtering}
Zone logic defines the queue area as a polygon on the frame and keeps only the people inside it. This avoids counting unrelated movement elsewhere in the scene and makes the measurement more relevant to the waiting line.

\subsection{Main Loop Integration}
The pipeline integration is handled in the runtime orchestration layer. The runtime supports:
\begin{itemize}
    \item handling local files, webcams, and live camera streams,
    \item adjusting how often frames are processed so the system stays responsive,
    \item showing detections, IDs, the queue zone, and metrics on screen,
    \item running without a visible interface when needed for later integration.
\end{itemize}

The sprint left behind a stable perception baseline that later sprints could build on for queue analytics and automation integration \cite{opencv_docs}.

\section{Conclusion}
Sprint 1 delivered the first working perception layer: people can be detected, followed across frames, and counted only inside the queue area. That gives Sprint 2 cleaner input for rate estimation and wait-time analysis.
